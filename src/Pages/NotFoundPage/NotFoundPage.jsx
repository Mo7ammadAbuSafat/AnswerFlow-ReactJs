import React from "react";

const NotFoundPage = () => {
  return (
    <div
      style={{
        height: "100vh",
        width: "100%",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        fontSize: "100px",
      }}
    >
      error 404
    </div>
  );
};

export default NotFoundPage;
